{
  "name": "Ultimate Dev Environment - Ubuntu 24.04",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "remoteUser": "root",
  "postCreateCommand": "cp -r .devcontainer/configs/.zshrc ~/.zshrc && cp -r .devcontainer/configs/nvim ~/.config/nvim && echo ✅ Ubuntu Dev Env Ready!",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-vscode.cpptools",
        "esbenp.prettier-vscode"
      ]
    }
  }
}
